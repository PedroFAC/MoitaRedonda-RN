const home = 'Home';
const signup = 'Signup';
const store = 'Store';
const productsAdmin = 'ProdAdmin';
const productForm = 'ProductForm';

const routesEnum = {
  home,
  signup,
  store,
  productsAdmin,
  productForm,
};

export default routesEnum;
